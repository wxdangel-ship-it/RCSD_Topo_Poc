# 10 Quality Requirements

- 可审计：每个 case 都有 status、audit、PNG
- 可批量：Anchor61 必须一次性跑完并稳定落盘
- 可扩展：当前代码结构允许后续 `Step6-7` 继续吸收，但本轮质量门禁覆盖 `Step4-5`
- 可诊断：`review` 与 `not_established` 必须给出显式原因
- 可治理：模块文档面、入口注册与项目治理文档保持一致
