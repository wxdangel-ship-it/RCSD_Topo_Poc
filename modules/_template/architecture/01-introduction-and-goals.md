# 01 Introduction And Goals

## 上下文

`<用 1-2 段说明模块为什么存在、处在项目主链哪个位置、解决什么业务问题。>`

## 目标

- `<架构目标 1>`
- `<架构目标 2>`
- `<架构目标 3>`

## 当前范围

- `<模块当前正式承接的流程、步骤、输入形态或输出形态。>`

## 兼容边界

- `<历史命名、兼容输出、旧入口或旧数据形态如何被当前文档解释；无则写“暂无兼容边界”。>`

## 非目标

- `<明确不处理什么。>`
