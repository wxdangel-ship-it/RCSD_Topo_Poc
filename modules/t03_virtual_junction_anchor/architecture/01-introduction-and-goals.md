# 01 Introduction And Goals

## 目标

- 启动 T03 独立模块，承接 `Phase A / Step3 legal-space baseline`
- 以低结构债方式承载 Anchor61 `case-package` 的批量审查能力

## 未来兼容

- 当前实现只做到 `Step3`；代码与文档结构保留后续 `Step4-7` 的干净扩展位，但本轮不实现这些步骤。

## 非目标

- 本轮不进入 `Step4/5/6/7`
- 本轮不迁移 T02 的 cleanup/trim 补救链
- 本轮不输出最终 topology / serialization 产物
