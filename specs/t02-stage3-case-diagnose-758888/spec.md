# 758888 单 case Stage3 分镜诊断轮

- 本轮范围：`T02 / Stage3 / case 758888`
- 本轮目标：导出 `758888` 在当前实现下的 `Step1 ~ Step7` 中间结果与统一视域分镜图，帮助用户直接判断“从哪一步开始不符合预期”
- 非目标：不修算法、不改正式结果、不改 `accepted / review / rejected` 判定、不做 case patch
