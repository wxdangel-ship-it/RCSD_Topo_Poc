# Tasks

1. 定位 `758888` 官方 case-package 与当前单 case pipeline 产物。
2. 导出 `Step1 ~ Step7` 中间层所需的图层与审计字段。
3. 渲染统一 bbox 的 step PNG 与 step GPKG。
4. 生成每一步固定结构的说明 markdown 与总摘要。
5. 跑 `test_anchor61_baseline.py`，写入 baseline 安全检查。
