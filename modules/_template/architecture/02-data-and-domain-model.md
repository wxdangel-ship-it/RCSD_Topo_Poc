# 02 Data And Domain Model

## 1. 输入对象

`<说明模块正式输入对象，以及这些对象在业务上代表什么。>`

核心输入对象包括：

- `<input>`：`<业务含义。>`

## 2. 业务对象与关系

`<说明模块真正处理的业务对象，不要只写文件名。>`

- `<business object>`：`<含义。>`

## 3. 领域分层

`<如果模块存在 relation、surface、segment、restriction、evidence、carrier 等分层，在这里说明每层回答什么业务问题。>`

## 4. 几何与字段语义

- CRS：`<EPSG:xxxx 或模块约定。>`
- `<field>`：`<业务含义、适用范围、缺失边界。>`

## 5. 下游数据语义

`<说明输出到下游后如何被解释，哪些字段不能被下游误用。>`
