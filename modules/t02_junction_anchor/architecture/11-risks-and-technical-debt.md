# 11 风险与技术债

## 当前主要风险

- T02 已被登记为正式模块，但上游 T01 仍未在 repo 级生命周期中同步重分类，存在治理层级不完全对齐的风险。
- 当前 stage1 只覆盖 gate，不覆盖 stage2；后续如果扩展不受控，容易把 stage2 逻辑误塞进 stage1 契约。
- 当前实现使用轻量 GIS 栈，虽然足以支撑 stage1，但在更大规模数据上可能需要更强的 IO / 空间索引插件支持。

## 当前可接受技术债

- `architecture/overview.md` 继续保留为概览入口，与标准 architecture 文档组并存。
- `specs/t02-junction-anchor/*` 仍保留在活动变更工件区，尚未归档。

## 后续缓解方式

- 后续若正式启动 stage2，应先以新的变更工件和模块契约收口，再进入实现。
- 若引入新的 GIS / IO 插件栈，应同步补环境依赖说明与内外网安装方案。
- 后续可考虑将 stage1 的输入层、空间判定层与输出层进一步拆分，降低单文件演化压力。
