# <module_id> 模块需求

## 1. 模块定位

`<用业务语言说明模块解决什么问题、处在主链哪个位置、服务哪个下游。>`

## 2. 业务目标

- `<目标 1：业务上要完成什么。>`
- `<目标 2：要把什么输入转成什么输出。>`
- `<目标 3：对下游提供什么稳定能力。>`

## 3. 当前范围

### 3.1 正式支持

- `<支持范围。>`

### 3.2 当前非目标

- `<明确不做什么。>`

## 4. 上下游关系

| 方向 | 模块 / 数据 | 关系 |
|---|---|---|
| 上游 | `<upstream>` | `<本模块消费什么成果。>` |
| 下游 | `<downstream>` | `<本模块输出给谁使用。>` |
| 支撑 | `<optional>` | `<可选证据或支撑输入；无则删除本行。>` |

## 5. 输入

| 输入 | 用途 |
|---|---|
| `<input>` | `<为什么需要它，支撑什么业务判断。>` |

## 6. 输出

| 输出 | 用途 |
|---|---|
| `<output>` | `<表达什么业务结果、供谁消费。>` |

## 7. 关键业务步骤

| 步骤 | 业务说明 |
|---|---|
| `<Step1>` | `<用中文说明该步骤解决什么业务问题。>` |
| `<Step2>` | `<说明判定、转换、锚定、融合、替换、恢复或编排逻辑。>` |
| `<Step3>` | `<说明输出和审计如何形成。>` |

## 8. 核心场景概念

`<如果模块有 A/B/C、Phase、Step、状态、分类、证据层等关键概念，必须在这里说明它回答什么业务问题，以及与下游状态的关系。没有则写“当前无独立核心场景概念”。>`

## 9. 什么是对

- `<正确判定 / 正确输出 / 正确审计要求。>`
- `<关键状态字段之间的正确关系。>`

## 10. 什么是错

- `<错误推断 / 错误输入使用方式 / silent fix。>`
- `<容易混淆的状态或历史命名误用。>`

## 11. 当前治理缺口

- `<当前仍需后续确认或治理的问题；无则写“暂无”。>`
