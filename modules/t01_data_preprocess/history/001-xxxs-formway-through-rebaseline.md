# T01 XXXS 外网样本 Through 规则纠偏记录

## 1. 已确认事实：本次纠偏背景

- 本轮针对外网 `XXXS` 样本执行 `Step1 Pair` 复核。
- 初始实现曾临时把 `cross_flag=2` 用作 through 识别条件，以拟合 `XXXS` 上的人工有效 pair。
- 后续经用户补充确认，该做法不具备上游字段语义依据，不可作为正式规则保留。

## 2. 已撤回规则：错误启发式

- 已撤回口径：`cross_flag=2 -> through node`
- 撤回原因：
  - 仓库内没有 `cross_flag` 的正式字段字典或项目级源事实说明。
  - 该规则来自局部样本与人工真值的拟合，而非已确认输入语义。
  - 该规则不满足泛化性与可解释性要求。

## 3. 用户补充确认：509144398 的关键语义

- 目标语义节点：`509144398`
- 关联 Road：
  - `509235032`
  - `607235228`
  - `510589257`
  - `604234858`
- 用户补充确认：
  - `509144398` 上存在两条右转专用道属性。
  - 去除右转专用道后，`509144398` 不再构成一个路口，只剩二度连接的 Road。

## 4. 已确认新口径：formway 正式启用

- `Road.formway` 已正式启用为 T01 当前可依赖输入字段。
- 当前已确认可用位语义：
  - `bit7` = 右转专用道
- 当前 Step1 through 判定改为：
  - 允许按策略先排除命中指定 `formway` 位的 Road
  - 再按排除后的 incident road degree 判断是否属于 through

## 5. 当前实现口径：XXXS case 的整改结果

- 当前策略配置对 through degree 使用：
  - `incident_road_degree_eq = 2`
  - `incident_degree_exclude_formway_bits_any = [7]`
- 对 `509144398`：
  - 原始 incident road degree = 4
  - 排除 `formway bit7` 后的 incident road degree = 2
- 因此当前将其识别为 through node 的依据，不再是 `cross_flag`，而是“排除右转专用道后的有效 incident degree”。

## 6. 已确认约束：后续处理原则

- 禁止根据局部样本、人工真值或单次冒烟结果，自行反推未启用字段的业务语义并直接固化为强规则。
- 若样本现象与既有语义冲突，必须先回到数据分析，并与用户确认后再调整实现。
- 字段一旦正式启用，必须同步写入项目级约束与模块契约，说明其当前适用语义与未确认边界。
