# SPEC：RCSD_Topo_Poc 项目级简版需求

- 文档类型：项目级需求简版
- 详细版需求：`docs/PROJECT_REQUIREMENTS.md`
- 详细方案策略：`docs/architecture/03-solution-strategy.md`
- 状态：Draft

## 1. 项目目标

`RCSD_Topo_Poc` 的目标是把 SWSD 的现场语义道路能力迁移到 RCSD / F-RCSD 承载网络中。项目不是单纯生成某个空间图层，而是形成一条可审计的数据融合链：先建立 SWSD-RCSD 语义路口关系，再基于这些关系替换 Segment，最后在 F-RCSD 上恢复路口通行规则。

## 2. 主业务链

```text
T08 -> T01 -> T07 -> T03 -> T04 -> T05 -> T06 -> T09
```

| 层级 | 模块 | 简要职责 |
|---|---|---|
| 输入准备 | T08 | 预处理 SWSD / RCSD，显性化 restriction、Laneinfo 和数据质量问题。 |
| Segment 基础 | T01 | 将 SWSD Road/Node 组织成后续可替换的 Segment。 |
| 路口关系 | T07 / T03 / T04 / T05 | 构建并发布 SWSD-RCSD 语义路口关系。T07 处理已有路口面锚定并保留可选兼容 relation 补锚，T03 处理交叉 / T 型，T04 处理分歧 / 合流 / 复杂路口，T05 统一发布 relation。 |
| Segment 替换 | T06 | 基于 T01 Segment 与 T05 relation 判断可替换性，处理 RCSD 数据质量和 SWSD/RCSD 工艺差异，输出 F-RCSD Road / Node。 |
| 人工审计 | T11 | 在 T10 编排中于 T06 后、T09 前抽取 relation repair candidates 与人工模板；只读审计，不改变 T06 到 T09 的业务数据依赖。 |
| F-RCSD 质检 | T12 | 对原始 1V1 匹配生成的 F-RCSD 做 SWSD 可达性等价假设检查，以 raw endpoint topology 为主证据。1V1/T05 选中 `base_id` mainNode 的 canonical raw alias group 先展开为实际 Road endpoint；其它显式 grouped raw node 保留但不递归展开各自 group。随后按当前方向筛选 source outgoing / target incoming；锚定 alias 距离只作审计，非锚定 spatial fallback 仍受原门禁。并以 portal-constrained semantic carrier 和 T07 Road-surface portal carrier 排除其余 raw 假断裂；后者要求唯一标准路口面、方向正确的物理 Road 及 Road-surface 或锚点一跳 frontier 证据。锚点可信度通过后自动发布高置信问题及排除证据，人工复核仅作可选 QA 覆盖，不修改 T06 或目标路网。 |
| 通行恢复 | T09 | 基于 SWSD restriction / Laneinfo 与 T06 F-RCSD 承载关系恢复路口级通行规则。 |
| 编排证据 | T10 | 组织 Case package、Case replay、full pipeline manifest、T06 funnel、可选 T12 quality audit、T11 candidate audit、feedback 和 visual check；不替代 T01-T09 / T11 / T12 算法。 |
| 神经直出 POC | P05 | 当前采用方案 A：冻结 T01 Junction—Segment 骨架和 PhysicalMovement 存在性，模型只负责 carrier 评分/选择、异常线索与失败概率；证据冲突按最小依赖闭包 fallback，不改写业务骨架。P13-P0 已完成并判定 `P05_SCHEME_A_P2_P3_P13_P0_SELECTION_NO_GO`，当前无可发布神经模型；M1/M2R/R2/RoadGraph PTO-P0 与 JSG-PTO-P0/P1/P2/P3 仅保留为历史实验结论。 |

T10 v1 Case runner 默认编排 `T01 -> T07 Step1/2 -> T03 -> T04 -> T05 -> T06 -> T11 -> T09`；显式启用 T12 时执行 `T06 -> T11 -> T12 -> T09`。T12 与 T11 都是 audit-only，T09 业务输入仍直接来自 T06。T10 另提供固定跳过 T08、启用 T12 的 F-RCSD 质量检查专用入口。T08 是独立前置预处理、质检和修复模块，不由 Case runner 调用；内网全量总控可把 T08 作为独立阶段串入全量链路。T07 Step3 是可选兼容 relation 补锚，不是 T10 默认主链阶段。

## 3. 核心业务需求

1. 输入数据必须先被整理成可被下游稳定消费的 SWSD / RCSD / 道路面 / 导流带 / restriction / Laneinfo 证据。
2. SWSD Road/Node 必须形成有方向、pair、junc 和 road body 语义的 Segment，作为 T06 和 T09 的基础。
3. T07、T03、T04、T05 必须共同建立可被 T06 消费的 SWSD-RCSD 语义路口 relation，避免下游重复解释路口关系。
4. T06 不能只按“有 1:1 relation”机械替换 Segment，必须处理 RCSD 端点、方向、道路切分、提前右转、内部短连接、surface 证据和 topology 连通等真实数据差异；surface 证据只能在 T04 未 reject、Patch 无冲突、证据可解释且 topology 回退通过时，将 retained-junction 距离 gate 降级为风险释放。
5. T11 必须在 T10 工作流中于 T06 后抽取人工 relation 修复候选，且不得把候选解释为人工确认或 T09 业务输入。
6. T09 必须在 F-RCSD 承载关系上恢复 SWSD 现场通行规则，并保留可追溯的 restriction、Laneinfo 和风险审计链。
7. T12 必须把“SWSD 与原始 1V1 F-RCSD 的通行性应等价”作为待验证质量假设；1V1/T05 锚定 mainNode 时，只展开选中 `base_id` 所代表 canonical group 的 raw subNode/alias，显式 grouped raw node 保留但不递归扩展各自 canonical group。成员必须落回 raw Road endpoint 图并按当前方向筛选 source outgoing / target incoming，不能用 canonical 零成本折叠代替物理 Road。锚定 alias 的 portal/标准面距离只作审计；非锚定 spatial node 仍执行既有范围/标准面门禁。raw endpoint failure 还必须依次排除受信 portal-constrained semantic carrier 与 T07 Road-surface portal carrier 后，才能结合标准路口、方向/几何和锚点可信度门禁自动进入 confirmed 正式问题层。两端唯一 T07 标准面锚定正确时，Road 与标准面的相交或锚点组一跳物理 Road 证明的 frontier 可作为 surface portal；一跳 support Road 必须从 anchor 向 frontier 有向并与标准面相交或满足 `1m` 拓扑容差，整条 carrier 至少一端必须有实际 Road-surface contact，不能以双端任意一跳邻接代替。其余距离类门禁只作审计，不能单独拒绝，但方向、物理 Road、锚点唯一性和路径长度等价仍是强门禁。两类 carrier 都只允许排除 raw 假断裂，不能靠零长度 canonical 折叠或无物理 Road 路径确认连通；外部 review decisions 仅作可选 QA 覆盖，任何结果都不得据此 silent fix。

## 4. 当前模块生命周期

- Active 正式业务模块：`t01_data_preprocess`、`t03_virtual_junction_anchor`、`t04_divmerge_virtual_polygon`、`t05_junction_surface_fusion`、`t06_segment_fusion_precheck`、`t07_semantic_junction_anchor`、`t08_preprocess`、`t09_swsd_field_rule_restoration`、`t10_e2e_orchestration`、`t11_manual_relation_review`、`t12_frcsd_quality_audit`
- Active POC / 成果模块：`p01_arm_build`、`p02_wuhan_local_experiment`、`p04_road_direct_generation`、`p05_neural_road_generation`
- Retired 模块：`t02_junction_anchor`
- Support Retained 模块：`t00_utility_toolbox`

模块生命周期事实以 `docs/doc-governance/module-lifecycle.md` 为准；模块业务盘点以 `docs/doc-governance/current-module-inventory.md` 为准。

## 5. 范围与非目标

项目级范围：

- 维护主业务链、模块职责、跨模块 handoff 和质量边界。
- 维护文件证据包、summary、audit、review、Case replay 和内外网协作口径。
- 维护已登记模块的生命周期与项目级治理缺口。

项目级非目标：

- 不在根目录文档展开模块内部算法、字段、参数、阈值和入口教程。
- 不把 T10 feedback 直接变成 T06 Step3 替换白名单。
- 不把 P01 POC 结果直接提升为 T09 正式契约。
- 不把 P02 武汉局部实验结果直接提升为全量生产口径，也不让 P02 替代 T08/T01/T05/T06 正式职责。
- P04 当前以 T01 Segment 为顶层业务单元，按 T07/T03/T04/T08 的 accepted JunctionUnit 和 Patch Vector 高精证据独立构建 Road/Node/RoadNextRoad；冻结 Directional V2 与 High-Precision V3 仅作历史对照。不得把 P04 单 Case POC 候选直接提升为正式 RCSD/F-RCSD，也不得让 P04 在多 Case 真值、restriction/ReferenceLane 完整 movement 合法性和正式化审批完成前替代 relation-first 正式主链。
- P04 在 SWSD 参考轴明显包含大段 Junction 内部长度时，可由已通过冲突占用和 DriveZone 门禁的 Patch access-surface候选证明两个不同 accepted endpoint surface之间的短高精物理走廊；仅当两个 `accepted surface + junction_endpoint_buffer`保护区彼此可区分、观测与补齐比例满足门槛、双方向原子实例化且全部拓扑/几何hard gate通过时，才可绕过SWSD长度覆盖门槛。保护区重叠或同一短线无法区分两端Junction时必须保留，不得用Review强行接管。
- P04 已选高精走廊到endpoint surface的直线补齐若离开合法道路域，但端点距离仍在既有缺失比例上限内，可在局部`RoadSurface ∪ accepted endpoint surface`内搜索受约束路径；路径必须满足覆盖、绕行、平滑后合法域和几何hard gate，不得引入SWSD坐标或远端路网绕行。由该新Road触发的LaneTopo切分若产生位于Segment两端面之外的主干尾段，只在同一父carrier另有唯一片段贯穿两个端点面时抑制该尾段并发布审计。
- P04 闭域验收采用三层合同：`BaselineCohort`由原始输入确定性计算且历史分母永久保留；`DirectBuildEligibility`只能由外部、可哈希、逐对象可审计的确认清单把对象分为`direct_build_required / patch_data_insufficient / reality_change`，默认均为`direct_build_required`；`PublishDisposition`独立表达最终高精发布、资料不足保留、现实变化待标准化或冲突保留。退出DirectBuild硬分母不等于退出发布，全范围Segment仍须完整发布Road/Node/RoadNextRoad，且报告必须同时披露原Baseline与DirectBuild两套分母。
- 不把 P05 的实验标签、模型指标或几何 fallback 直接提升为 T01-T06 正式规则；P05 R2 可以把人工确认的 T03/T04/T05/T06 产物和 oracle edit payload 作为 label-only 监督，但最终目标仍是 T06 Step3 Road/Node，缺失任务必须 masked，truth-derived payload 不得成为推理输入。
- P05 PTO-P0 允许登记 commit、输入 manifest 与 lineage 完整的 T03-T06 策略重放生成候选；登记历史 T10 replay 中可保留 T07 可选辅助 stage，但它不形成独立候选或最终选择。候选层不得读取 truth/oracle；正确候选由冻结后的 label-only Oracle cost 选择，只用于验证 candidate reachability 与约束 formulation，不得解释为模型泛化能力。
- P05 PTO-P0 已证明候选与 formulation 在 51 Case 上语义可行，但当前全链 replay P95/max 超过冻结预算；PTO-P1 仅允许先使用冻结/缓存候选做 grouped OOF learned scoring，生产化前必须另行验证轻量或增量 proposal generator 的端到端成本。
- P05 `P05-JSG-PTO-P0` 已完成：P0 构建 Junction/StandardSegment/JunctionSegmentRelation/PhysicalMovement/SegmentConnector/Terminal/loop 本体、label-only Oracle truth、evaluator 和确定性 compiler；51/51 Case 往返与编译精确，hard failure 为零，两轮 signature 一致。Road/Node 是编译载体，R2 edit/materializer 是后端 IR。该结论不得解释为未来推理候选、scorer、生产接入或新的上游强规则已经成立。
- P05 当前已完成 `P05-JSG-PTO-P1`。两轮 51 Case 均使用零 truth 候选层，候选 417,493 个、分组 72,318 个；PTO-A/PTO-B 均 51/51 `OPTIMAL`、gap=0，编译 RoadGraph 51/51 精确，hard failure、relaxation、content repair、silent fix 均为零，五类确定性 signature 一致。P1 未训练 scorer、未修改 T01-T09；历史 strategy replay 的 5,751.192s 成本继续保留为在线 proposal 性能 NO-GO。
- P05 `P05-JSG-PTO-P3` 已完成并正式判定 `P3_MODEL_NO_GO`。51 Case、3 seeds × 5 folds 中 JSG Top-1/macro 为 `0.9390~0.9395 / 0.8471~0.8817`，证明 object-conditioned scorer 明显优于 P2；但 Connector 仅 `0.4283~0.5992`，Review/Unknown recall/precision 仅 `0.4389~0.4952 / 0.6886~0.7828`，未达主门禁。三个 seed 的 PTO/RoadGraph safety gate 均完整通过。该结论只作为历史模型证据；历史 proposal replay 与生产/在线仍为 NO-GO。
- P05 当前方案 A 已于 2026-07-22 授权：T01 Segment 集合、Junction 关系与 PhysicalMovement 存在性是冻结输入，神经模型不得新增、删除、合并、拆分或改归属，只能对 Road/Node carrier 方案评分/选择并报告异常。正式 Segment 必须拥有独立 Road；普通提右统一为 `segment_type=ADVANCE_RIGHT` 的 Segment，当前业务层不再使用 `SegmentConnector`。
- P05 证据冲突必须形成 `RealityChangeClue` 并进入失败/fallback：Junction 冲突回退该 Junction 关联的全部 Segment，Segment 冲突只回退该 Segment且不自动改变或回退相关 PhysicalMovement；Movement 仅因自身问题回退，carrier 确实共享或影响 Junction 内部拓扑时才升级为 Junction fallback，否则仅回退该 Movement。fallback 只有在保留 SWSD 后仍满足统一业务本体和拓扑 hard gate 时才计成功。旧 PTO-A 不得改写当前业务骨架，旧 Connector 与 Review/Unknown 指标不再是当前业务验收指标。
- P05 Scheme-A-P1 的 RoadGraph 安全门按“51/51 有确定终态”验收，不把原始 SWSD 非法伪装为成功。冻结范围内 `T10:74155468` 与 `T10:609214532` 已登记为 SWSD baseline 端点 Node 缺失的预期失败：必须输出 `FAIL + RealityChangeClue`、禁止发布和修复；其余 49 Case 必须形成合法 RoadGraph。两个预期失败仍保留在51 Case模型与异常指标分母中，不属于排除项。
- P05 方案 A baseline 已完成：按已确认的“Segment 不连带 Movement”口径重跑的正式双跑 `p05_scheme_a_baseline_20260722_12/_13` 覆盖 51 Case、8,863 Segment、474 ADVANCE_RIGHT 和 24,779 PhysicalMovement，骨架 mutation 为零、五类业务 signature 一致。40 个 access 不可确认 ADVANCE_RIGHT 显式失败/mask；旧 `_10/_11` 仅保留为修正前历史证据。该阶段只完成数据与安全执行合同，尚未训练方案 A scorer。
- P05 `P05-Scheme-A-P1` 已于 2026-07-22 正式完成并判定 **`P05_SCHEME_A_P1_MODEL_NO_GO`**。正式 run `p05_scheme_a_p1_oof_formal_20260722_01` 的三 seed Segment macro-F1 为 `1.0000/1.0000/0.9869`、Movement exact 均为 `1.0`、Gate 4/5 通过；但 accepted coverage 仅 `0.3637/0.3589/0.3533`，且 seed 29/43 anomaly precision 为 `0.7684/0.7472`，未过 Gate 1/2/3。truth-exact 执行 coverage 也只有 `0.36933`，说明主要瓶颈是逐对象 carrier truth 的整图来源一致性，而不是扩大同一 scorer 容量。该结论不授权生产接入，不改变冻结业务骨架。
- P05 `P05-Scheme-A-P2-P0` 已于 2026-07-22 完成，正式结论为 **`P05_SCHEME_A_P2_P0_UPSTREAM_CARRIER_NO_GO`**。本阶段冻结 Movement，把 Segment 独立 Road carrier 与 JunctionUnit 共享 Node carrier 分层；truth-free candidate、49+2 RoadGraph 安全、双跑确定性和资源门均通过，joint truth exact coverage 为 `0.546542`，但当时受限 carrier bundle 的 `USE_RCSD` truth retention 仅 `0.165753`，低于该阶段 `0.50` 门槛，故未启动 P2-P1 训练。该历史结论不否定逐 Segment 神经评分能力，也不修改 T01–T12、不新增入口、不接生产；其数据可达性含义以后续 Dataset-P0 为准。
- P05 `P05-Scheme-A-Dataset-P0` 已于 2026-07-22 正式完成并判定 **`P05_SCHEME_A_DATASET_P0_GO`**。本阶段按模块职责重建 741 sample、520 artifact、11,856 task target、51 Case 与 8,863 Segment 的训练合同：T01 只提供冻结 SWSD Segment 骨架和 fallback，T07 固定 `DRIVEZONE_ONLY`，T03/T04/T05 是 label-only 中间监督，T06 Step3 Road/Node 是最终主标签，Movement candidate/decision/evaluation 均为零。正式双跑证明 2,190/2,190 `USE_RCSD` Segment 的正确 Road 可由非 T01 truth-free candidate 覆盖，23,224 Road、27,553 Node 与 8,823 个可用 Segment 联合可达率均为 `1.0`，并保持 49 `LEGAL` + 2 `EXPECTED_FAIL`。因此历史 P2-P0 的 `0.165753` 只表示当时受限 carrier bundle 的联合安全保留率，不表示本地 Case 缺少正确 RCSD 数据，也不表示 T01 应生成 RCSD candidate；离线候选可达性已 GO，但该结论不自动授权 scorer 训练或在线/生产接入。
- P05 `P05-Scheme-A-P2-P1` 已于 2026-07-23 完成，正式判定 **`P05_SCHEME_A_P2_P1_SAFETY_NO_GO`**。Road endpoint/JunctionUnit 条件化 Node 真值、77,964 条 truth-free compatibility edge 与 51/51 compatibility Oracle 均通过；独立 Node top-1 约 `0.7558`，联合 Node exact 提升至 `0.9965~0.9985`，三个 seed 都保持 49 `LEGAL` + 2 `EXPECTED_FAIL`。但错误自动接受仍为 `17/9/17`，总体 coverage=`0.3102/0.3502/0.5150`、`USE_RCSD` coverage=`0.0999/0.0027/0.2658`，anomaly precision=`0.3460/0.2851/0.3936`；seed 43 Segment macro-F1=`0.8190`。双跑、GIS/CRS、资源、体量和入口门通过。该结论证明条件化 carrier 方案有效，但当前自动接受/异常校准不安全；不得接在线 proposal 或生产主链。
- P05 `P05-Scheme-A-P2-P2-P0` 已于 2026-07-23 完成，正式判定 **`P05_SCHEME_A_P2_P2_P0_CALIBRATION_NO_GO_SAFETY_HEAD_GO`**。只读审计将 P2-P1 的对象级 accepted wrong `17/9/17` 追踪为 raw Segment 错误 `12/10/37` 和真正 accepted Segment 根错误 `2/0/3`；其余 Node 行已区分为 accepted Segment 传播、rejected Segment 的 fallback 前信号、structural fallback 改正或独立/未引用 Node。8 个稳定 false-use 无法用单一 probability/margin/entropy/anomaly 阈值在零错误下保留超过 `0.200275` 的正确 `USE_RCSD`，低于 `0.50`；8,863 Segment 的完整 truth-free feature 未发现跨 truth 精确碰撞。该阶段未训练模型，只证明 calibration-only 不足、独立 Case-grouped safety/abstention head 具备技术启动理由；P2-P1 NO-GO、40 Review 禁止自动发布和生产边界不变。
- P05 `P05-Scheme-A-P2-P2-P1` 已于 2026-07-23 完成，正式判定 **`P05_SCHEME_A_P2_P2_P1_MODEL_NO_GO`**。410,786 参数的 Segment-only safety head 采用 3 safety seeds × 5 Case folds，并冻结 P2-P1 candidate/base score；错误接受/总体覆盖/`USE_RCSD` 覆盖分别为 `5/0.374817/0.431714`、`0/0.069841/0.066911`、`4/0.296288/0.380843`，没有 seed 同时达到零错误和两个 `>=0.50` 覆盖门槛。40 Review 自动发布均为零。Node 条件化与 RoadGraph 每 seed 均为 49 `LEGAL` + 2 `EXPECTED_FAIL`，effective requirement conflict/mismatch、payload conflict、unexpected failure 均为零；正式双跑内容确定。该结论否定当前 safety-head 自动发布能力，不否定候选可达性、整图方案或神经逐对象排序价值；不得在已见 held-out 上继续调阈值后重报 GO，也不授权在线/生产接入。
- P05 `P05-Scheme-A-P2-P2-P2-P0` 已于 2026-07-23 完成，正式判定 **`P05_SCHEME_A_P2_P2_P2_P0_EVIDENCE_NO_GO`**。本阶段冻结 P2-P1/P2-P2-P1，只把 T01 骨架、T07 `DRIVEZONE_ONLY`、truth-free proposal/KEEP Road 有向结构差、Segment→Node compatibility/Junction共享压力和 base OOF 统计组成 202 维推理证据；T03/T04/T05/T06 model-input、truth/ID/绝对坐标和 Movement feature 均为零。203 参数线性 probe 仍自动放过 2/9 个一致错误 proposal；15,105 参数浅层 MLP 的全局 accepted wrong/Review auto 为 `0/0`，但 unsafe recall=`0.994191` 且 5 个 held-out fold 无一同时达到零错误、recall=`1.0` 和总体/`USE_RCSD` coverage 均 `>=0.50`。两个 probe 的 Node/RoadGraph 均保持 49 `LEGAL` + 2 `EXPECTED_FAIL` 且 conflict/mismatch/新增失败为零；正式 Run A/B signature=`b04485a71f05df15d36135a3193edcf8db150855ae24878b435faead028142e3`。当前允许的结构证据不足以启动自动发布模型，只保留离线排序/Review/RealityChangeClue 辅助价值；继续研究必须引入新的推理期信息源或独立预训练表征，label-only 字段提升仍须用户二次确认。
- P05 `P05-Scheme-A-P2-P2-P2-P1` 已于 2026-07-23 完成，正式判定 **`P05_SCHEME_A_P2_P2_P2_P1_SOURCE_FACT_BLOCKED`**。对 9 个一致错误 proposal、浅层 MLP 仍接受的 13 个 unsafe Segment 和 40 Review 合并后的 62 个唯一对象完成逐对象直接归因：40 个 Review 已由 T01 `ADVANCE_RIGHT access_valid=false` 推理硬门解释；其余 22 个分别为 16 个 truth-conditioned Junction fallback、5 个 T06 carrier Road 缺失和 1 个 T06 `MIXED_CARRIER`，直接事实均只存在于当前 label-only T06/联合真值层。`UNOBSERVABLE_FALLBACK=0`，但新增且已授权的直接推理证据仍为 0；P2-P1 joint fallback 的任一 seed unsafe precision 仅 `0.2083~0.2981`，只能作为辅助线索。不得据此直接提升 T06 终态字段、启动训练或宣称替代 T06。
- P05 `P05-Scheme-A-P2-P2-P2-P2` 已于 2026-07-23 完成，正式判定 **`P05_SCHEME_A_P2_P2_P2_P2_PARTIAL_ROUTE_NO_MODEL_GO`**。旧 `unsafe` 指标被拆为 Road/Carrier 安全与 RealityChangeClue 可见性：浅层 MLP 的 carrier accepted wrong=`0`、carrier safety recall=`1.0`，此前 13 个残留对象均为 `KEEP_SWSD→KEEP_SWSD` 的 clue-only 漏报；但逐 fold 仍只有 `2/5` 通过。22/22 正确候选均可达：16 个对象属于 Junction 一致性依赖，5 个对象候选集合只有 KEEP/Review，1 个 `MIXED_CARRIER` 正确候选已存在但评分选错；初始 26 个 Node payload 冲突确定性闭包为 57 个 Junction fallback Segment，target 为 `36 KEEP/13 MIXED/8 USE`。这证明本地数据支持分层 carrier/clue/Junction 研究路线，但现有单层浅层 MLP 仍 NO-GO；T03/T04/T05/T06 继续只作监督/归因，未提升为推理输入。
- P05 `P05-Scheme-A-P2-P3-P0` 已于 2026-07-23 完成，正式判定 **`P05_SCHEME_A_P2_P3_P0_MODEL_NO_GO`**。2.818M 参数分层模型按 3 seeds × 5 Case folds 联合训练 candidate carrier、candidate correctness、RealityChangeClue 和 7 个 T03/T04/T05 辅助目标；推理仍只消费 P2-P1 truth-free candidate 与冻结 202 维 T01/T07/结构证据，T03–T06 终态输入、ID、绝对坐标和 Movement feature 均为零。通用 Node compatibility/Junction decoder 在三个 seed 上均达到 49 `LEGAL` + 2 `EXPECTED_FAIL`，冲突、错配、repair 和骨架 mutation 为零，证明整图合法生成链路成立；但 seed 311/313 各稳定错误接受同一 `KEEP_SWSD→USE_RCSD` Segment，fold 2 三 seed coverage 仅约 `0.29/0.32 USE`，seed 317 以过度 clue/fallback 换取零错误。clue recall=`0.9844/0.9852/0.9987`，已知 13 clue-only 捕获=`9/8/12`，均未过门。正式 Run A/B signature=`d6974ccaa140442412cf793d1379dc3a3232a1bba9b874207dcb12d7faddff59` 且 Run B 内容匹配；不得在当前 held-out 51 Case 上继续调阈值、挑 seed 或提升 label-only 字段后重报 GO。
- P05 `P05-Scheme-A-P2-P3-P1` 已于 2026-07-23 完成只读失败归因与推理期证据审计，正式判定 **`P05_SCHEME_A_P2_P3_P1_EVIDENCE_NO_GO`**。seed 311/313 稳定误接受的是同一 Segment，直接原因是只存在于 label-only 联合真值层的 Junction fallback；13 个 clue-only 对象中 8 个同属 Junction fallback、5 个为 T06 `RCSD_CARRIER_ROAD_MISSING`，现有允许推理证据没有新增直接事实。fold 2 的 3,037 个 Segment 中 1,795 个来自登记的 SWSD baseline expected-failure Case，故把全部 Segment 作为自动化 coverage 分母时理论上限仅 `0.408956<0.50`；诊断性 eligible-only coverage 为 `0.7061~0.7150`，但 `USE_RCSD` coverage 仍仅 `0.3195~0.3239`。`E:\TestData\POC_Data` 的 741 个登记样本已全部进入现有监督/51 Case 或批准排除；其余 11 个 T10 anchor slice、9 个 T06 诊断包及旧 T01/T02/Intersection 包均不是独立、冻结、带最终 RoadGraph 真值的验证集。T07 Step1 明确为 DriveZone-only，RCSDIntersection 只用于 Step2；两类 T07 证据均已在当前合法推理证据合同中，不构成新增直接事实。正式 Run A/B `p05_scheme_a_p2_p3_p1_audit_20260723_04/_05` signature=`177344821e1b8b932a7b19bf16248ede1f6293d622c16570ba301ea9a7384311`，Run B 内容匹配。本结论不否定神经评分路线，但在新增合法直接证据与独立冻结验证集至少一项缺失时，不授权重启模型训练。
- P05 `P05-Scheme-A-Dataset-P1` 已于 2026-07-23 完成并判定 **`P05_SCHEME_A_DATASET_P1_GO`**。`T10` 保持 Case 级全量真值；`T10-Error/T10-Error-2` 只允许目录 manifest 的目标 Segment或其 lineage 可证明的当前 T01 后继 Segment作为标签，其它 Segment只作 `context_input_weight=0.3` 的输入上下文，不进入 label/loss/metric。45 个启用包形成 41 个 direct ID（含 5 个 Road drift 审计）和 4 个无歧义 Road partition lineage，后者映射 3/4/7/13 个当前 Segment；新标签/上下文分母为 `6,275/2,588`，上下文标签泄漏为 0。`T10:609214532` 与 `T10:74155468` 仍为 Case `EXPECTED_FAIL`，但每个 Case/seed 只定位 1 个实际失败 group，禁止再把全 Case 1,795/159 个 Segment级联屏蔽。正式 Run A/B `p05_scheme_a_dataset_p1_20260723_01/_02` signature=`bc848a8a0eeda04c14b358d505bc70258deaf36bb40cb617611ba7c4d205065c`，Run B 内容匹配。此前以 8,863 为标签分母的模型、stable-wrong、coverage、recall 和 macro-F1 结论只保留为旧口径历史证据；骨架、candidate inventory 和 49+2 图安全等独立事实继续有效。本 GO 不授权训练或生产接入。
- P05 `P05-Scheme-A-P2-P3-P2` 已于 2026-07-23 在用户继续授权后完成，正式判定 **`P05_SCHEME_A_P2_P3_P2_MODEL_NO_GO`**。本阶段保持 P2-P3-P0 的 2.818M 级网络、202维 T01/T07 推理证据、3 seeds × 5 Case folds和安全门不变，只将监督/指标重建为 Dataset-P1 的 6,275 个 eligible Segment；2,588 个 context-only Segment不进入 loss/threshold/calibration/metric并确定性 `KEEP_SWSD` fallback。三个 seed 的 accepted wrong/Review auto/总体 coverage/`USE_RCSD` coverage 分别为 `1/0/0.3540/0.6339`、`13/12/0.5495/0.7037`、`0/0/0.1506/0.2757`；clue recall/precision/macro-F1 分别为 `0.9805/0.5440/0.7751`、`0.8670/0.9977/0.9536`、`0.9960/0.3684/0.5879`。可靠目标 `T10-Error-2:89387685_507565991` 在 seed 311/313 被错误 `KEEP_SWSD→USE_RCSD`，seed 313 另将 `T10:605415675` 的12个 `ADVANCE_RIGHT` Review错误自动接受。每 seed仍为49 `LEGAL`+2 `EXPECTED_FAIL`，context auto accept、非目标Case级联、冲突、错配、repair和骨架mutation均为0。正式 Run `p05_scheme_a_p2_p3_p2_oof_20260723_04/_05` signature均为`e1bc5b5e55ddeaba8f87cbaa36f8a6261461e206a72aa8d240385c46c30d534f`且Run 05 reference match=true。结论说明标签修正是必要但不充分条件；当前基础 scorer 的安全校准与 Review 泛化仍不适合自动发布，不得挑seed、在本批held-out继续调阈值或接生产。
- P05 `P05-Scheme-A-P2-P3-P3` 已于 2026-07-23 完成，正式判定 **`P05_SCHEME_A_P2_P3_P3_SAFETY_GATE_GO_NEXT_REPRESENTATION_REQUIRED`**。6,275个eligible Segment与冻结方案A清单全量1:1匹配；恰好40个`access_valid=false`对象全部为`ADVANCE_RIGHT + REVIEW_FALLBACK`，非Review误触发为0。将该既有T01事实作为scorer后、Junction/Node闭包前的硬安全资格重放后，三seed Review auto从`0/12/0`降为`0/0/0`，最终accepted wrong为`1/1/0`，每seed仍为49 `LEGAL`+2 `EXPECTED_FAIL`。剩余可靠对象`T10-Error-2:89387685_507565991`三个seed均以`12.87~15.99` score margin错误选择`USE_RCSD`；每个seed按held-out训练Case建立的前20个202维近邻合计60个全部是`USE_RCSD`真值，说明当前表征把该对象稳定放入错误区域。正式Run `p05_scheme_a_p2_p3_p3_audit_20260723_02/_03` signature均为`0f7d4ee09835afb408efa986f54ed980ca941484a3ca62c7f3805f8d684fa97c`且Run B reference match=true。下一阶段应先建设T06之前可用的新表征并独立验收，不继续训练同一表征、调当前held-out阈值或进入自动发布。
- P05 `P05-Scheme-A-P2-P3-P4` 已于 2026-07-23 完成，正式判定 **`P05_SCHEME_A_P2_P3_P4_TRUTH_REBASELINE_GO_NO_RESIDUAL_REPRESENTATION_REQUIRED`**。P4 将真值构造顺序修正为“Dataset-P1 先冻结 6,275 label/2,588 context，再让 context 安全 `KEEP_SWSD`，最后计算 Node/Junction 闭包”；初始 Node payload conflict 从历史 26 降为10，Junction fallback Segment从57降为21（eligible 10），最终28,240个Node真值无冲突。相对旧P2-P1共436个Segment标签变化，其中435个为context-only、可靠标签只变化1个；原残余对象被从错误的`KEEP_SWSD + anomaly=true`恢复为`USE_RCSD + anomaly=false`，candidate=`sap1:918ffd80e766808f8a6b516c`。按既有P2-P3-P3决策重算后，三seed accepted wrong/Review auto均为`0/0/0`且carrier safety recall均为1.0，因此P2-P3-P3“残余对象要求新表征”的推断只保留为闭包顺序修正前历史结论；但safe coverage与clue precision/recall仍跨seed/fold不稳定，当前模型继续NO-GO。正式Run `p05_scheme_a_p2_p3_p4_rebaseline_20260723_01/_02` signature=`3f2f2399a11a1b4675bc5b30d29043e764bd7991a71c2d06f6fccbdde265ed37`且Run B reference match=true。本结论不授权训练；下一阶段只能另行授权后基于修正真值重训/复验。
- P05 `P05-Scheme-A-P2-P3-P5` 已于 2026-07-23 完成，正式判定 **`P05_SCHEME_A_P2_P3_P5_MODEL_NO_GO`**。P5 只用 P4 scope-first 修正真值从头重训同一 2.818M 级网络和 202 维 T01/T07 truth-free 证据，候选、模型结构、3 seeds × 5 Case folds 与安全层均未改变。P6 后续审计确认：P5 的 final publication wrong/Review auto均为`0/0`，但 scorer decision wrong accepted 实为`1/1/1`、carrier safety recall为`0.975610/0.975610/0.976744`；此前 safe coverage=`0.4290/0.5498/0.1374`是 RoadGraph 原子阻断后的发布覆盖，scorer 前置覆盖为`0.6524/0.7952/0.3469`。clue recall/precision/macro-F1=`0.9805/0.6614/0.8512`、`0.8831/0.9985/0.9596`、`0.9960/0.3605/0.5751`，未同时通过逐seed/逐fold门。每seed仍为49 `LEGAL`+2 `EXPECTED_FAIL`，冲突、错配、repair和骨架mutation为0。P5 `MODEL_NO_GO`不变；不得挑seed、在当前held-out上调阈值或自动替换SWSD。
- P05 `P05-Scheme-A-P2-P3-P6` 已于 2026-07-24 完成只读双层失败归因，正式判定 **`P05_SCHEME_A_P2_P3_P6_ATTRIBUTION_GO_DUAL_ROUTE_REQUIRED`**。每seed 6,275 eligible 中，safe coverage按冻结合同排除40 Review，以6,235为分母；两个`EXPECTED_FAIL` Case在scorer层只局部失败2个group，在final publication层则原子阻断1,954个eligible对象。三seed唯一稳定错误选择均为`T10:609214532 / 505101583_506183080`的`KEEP_SWSD→USE_RCSD`。clue FP/FN=`747/29、2/174、2629/6`，15个fold阈值跨`0.000296–0.998983`；3,587条clue error无相反标签精确证据碰撞，但稳定错误对象的top-20训练邻域均为`USE_RCSD + clue=false`。因此下一路线必须同时处理clue calibration和新的T06前truth-free表征。P6 GO不改变P5模型NO-GO，也不授权训练或生产接入。
- P05 `P05-Scheme-A-P2-P3-P7` 已于 2026-07-24 完成并判定 **`P05_SCHEME_A_P2_P3_P7_CURRENT_SOURCE_NO_GO`**。本阶段没有训练、拟合校准器或调阈值；历史202维证据保留不改写，并按用户授权实际剔除14个非零`MOVEMENT_DEGREE/CONTEXT_MOVEMENT_DEGREE`维及其28个邻域派生维，形成`188 + 377 + 37 = 602`维无Movement表征。6,275/6,275对象具有`EPSG:3857` T01相对几何，T03–T06/truth/ID/绝对坐标推理维均为0。稳定错误`T10:609214532 / 505101583_506183080`的新top-20训练邻域仍为`20/20 USE_RCSD + 20/20 clue=false`；recall固定为1时三seed最佳precision/macro-F1仅约`0.241/0.238`、`0.239/0.229`、`0.361/0.582`，单调calibration-only路线全部失败。正式双跑signature=`3154e4bb6af8358efcfff6f6dd5ed7ca90189f0d915d654d86fb1cbcdac2bcee`且Run B匹配。该结论表示当前已授权T06前来源不足，不表示神经网络整体不适用；不得启动下一训练轮，T03/T04推理角色提升或新增确定性T06前关系生成器必须另行获得业务授权。
- P05 `P05-Scheme-A-P2-P3-P8` 已于 2026-07-24 完成，正式判定 **`P05_SCHEME_A_P2_P3_P8_PARTIAL_GO_CARRIER_ONLY_CLUE_SOURCE_BLOCKED`**。51个eligible Case的T03/T04正式T05 handoff工件、hash和CRS通过，只以Case-local T01 `junc_nodes`精确关联6,275个Segment；504个对象适用T03/T04来源，192个对象命中多个来源，5,771个无适用来源并显式保留`NOT_APPLICABLE`。稳定carrier wrong命中T04 `no_related_rcsd`关系状态，held-out-fold之外存在2个方向归一后的完全同类对象，二者均为`KEEP_SWSD + clue=true`且`USE_RCSD=0`，故carrier来源具备字段promotion二次评审理由；但6个稳定Clue错误仅1个具有T03/T04适用证据，Clue来源门失败。正式双跑`p05_scheme_a_p2_p3_p8_source_audit_20260724_02/_03` signature=`4b3002494b6c33400907751aca44c375481a3602bb3cff1f8cad45bce8852508`且Run B匹配。T03/T04继续`model_input=false/label_only=true`，本结论不自动提升字段、不授权训练或生产接入。
- P05 `P05-Scheme-A-P2-P3-P9` 已于2026-07-24按批准合同完成，正式判定 **`P05_SCHEME_A_P2_P3_P9_PROMOTION_MODEL_NO_GO`**。602维Control与冻结Control后的carrier-only source residual adapter完成3 seeds × 5 Case folds和正式双跑；504个source-applicable对象的Control/Treatment pooled macro-F1与KEEP recall完全相同，均为`0.9986769935 / 0.99609375`，没有严格增益。稳定对象`T10:609214532 / 505101583_506183080`在三seed仍选择`USE_RCSD`，每seed scorer层错误自动接受均为1，safety recall为`0.97778/0.97778/0.97619`。5,771个无来源对象score/decision差异为0，Clue source feature/loss/decision和Clue概率差异均为0；RoadGraph每seed保持49 `LEGAL`+2 `EXPECTED_FAIL`且冲突/修复/mutation为0。Run A/B signature=`e8f19d737a27e5789ea861e18730f11d192a9b97635ca25a8fd4ac299f37871b`、Run B匹配，wall=`375.97s/355.44s`、RSS=`2.72/2.66GiB`、P95=`0.114/0.100s`，完整P05回归242项通过。P8历史partial-GO与字段白名单不改写，但本adapter方案不得提升为carrier模型GO、自动替换SWSD或生产接入；下一轮训练或架构变化需另行授权。
- P05 `P05-Scheme-A-P2-P3-P10` 已于2026-07-24按用户逐对象裁决完成冻结P9输出复算，正式判定 **`P05_SCHEME_A_P2_P3_P10_TRUTH_REBASELINE_GO_P9_PROMOTION_NO_GAIN`**。对象级人工真值权重1.0覆盖T10 Case级0.7：`609214532 / 505101583_506183080`为`USE_RCSD + clue=false`；`706247 / 706317_706319`最终只允许`KEEP_SWSD + clue=true`并执行Junction fallback；`706247 / 706346_706349`允许`KEEP_SWSD/USE_RCSD`、优先`USE_RCSD + clue=false`；`609214532 / 513242335_523239407`与`609214532 / 606102026_609617028`均为`KEEP_SWSD + clue=false`，其RCSD数据缺失不构成当前道路结构冲突。复算后Control/Treatment在504个source-applicable对象、三seed合计1,512条记录上的合法准确率均为1.0，优选命中率均为`0.9980158730`，三seed scorer wrong accepted、Review auto publish和Junction fallback violation均为0，carrier safety recall均为1.0。原“609稳定模型误选”解释失效；但Control与Treatment指标仍完全相同、严格增益为false，因此P9 adapter继续不得promotion。Clue pooled precision/recall/macro-F1为`0.583278/0.987197/0.804359`，FP/FN为`3140/57`，对象级三seed稳定Clue漏报已归零；P9冻结的逐seed/逐fold coverage门未因只读复算而改变，完整模型仍NO-GO。P10未训练、未改模型权重或P9工件；三对象中间Run `_01/_02`保留为历史证据，五对象正式Run `_03/_04` content signature=`ef779bfaf89c2bbfc0ef27d8e0e52cbd9075f145c9c54cf100c350bc0557d9cc`且Run D匹配，完整P05回归245项通过。
- 不继续扩展 Retired T02 的业务职责。

## 6. 近期改进重点

- 产品化 T07/T03/T04/T05 relation 的成功、fallback、review-only、blocked 和 upstream-needed 状态。
- 将 T06 problem registry 与 T10 feedback 形成稳定闭环，让可自动消费的问题进入 T05，其它问题进入人工复核或上游模块任务。
- 建立 T06 Step3 后的批量 QA 指标，覆盖 source 同源性、端点完整性、节点映射、提前右转挂接、surface closure、F-RCSD 连通和 T09 carrier 可用性。
- 用 T12 持续验证原始 1V1 F-RCSD 与 SWSD 的可达性等价假设，自动发布高置信正式问题，并冻结 raw topology、portal-constrained semantic carrier、T07 Road-surface portal、标准路口锚点可信度和误报排除证据。
- 结合 RCSD Laneinfo 与轨迹通行证据继续增强 T09。
