# Tasks

1. 只读确认 provenance 断链点与允许修改边界。
2. 在最小 export/render sink 上修复 geometry handoff。
3. 增加一条聚焦回归，锁定 exported GPKG metrics 与 Step6 metrics 对齐。
4. 跑最小测试集。
5. rerun 重点 case，生成 provenance fix 报告与对照材料。
