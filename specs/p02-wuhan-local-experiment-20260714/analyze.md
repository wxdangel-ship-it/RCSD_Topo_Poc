# P02 武汉局部实验实施前分析

## 1. 一致性结论

- 用户任务与项目 relation-first 架构一致：人工关系必须先进入 T05，再由 T06 消费。
- `closed_connect` 原始字段此前未启用；用户已确认与 `closed_con` 等价并授权同步源事实，解除 §1.5 阻断。
- 区域口径已从西安修正为武汉，解除坐标与命名冲突。
- 缺失端点已获确认；用户进一步要求不裁剪原始数据，完整 Road/Node 进入正式流程，缺失仅审计、不 silent fix。

## 2. 接口影响

- 不改变 T01 强规则值域；只补充 T08 输入别名归一。
- 不改变 T05 callable/CLI；使用显式空兼容工件进入现有 manual relation 路径。修复 T05 新增 RCSDNode 对大小写模板生成重复字段的内部缺陷。
- 不改变 T06 替换边界；仅在统一输入读取层增加字段键大小写归一和异值冲突阻断。
- P02 只新增模块内 callable，无 repo 官方入口。

## 3. 实施闸门

- Spec/Plan/Tasks 覆盖产品、架构、研发、测试、QA：通过。
- 源事实冲突：已由用户确认解除。
- 官方入口变更：无。
- 文件体量：实现前执行逐文件字节检查。
- 大范围迁移：无。

## 4. 允许进入 implement

当前任务书已就绪，可以进入测试先行与最小实现。

## 4.1 历史 run03 边界走廊修订（已被当前任务覆盖）

- 用户确认 `5855295910117379 / 5855295910117428` 是两个锚定路口之间的原始 RCSDRoad 连通事实，并授权修复。
- 原“非直接人工 Road 锚定的悬空 Road 全部剔除”规则与该事实冲突，现收敛为三重门控的工作副本端点归一：人工锚定 RCSDNode、`CrossLid` 命中、唯一精确端点重合。
- 修订只影响 P02 local clip；T05/T06 契约补充“不自行反推 `CrossLid` 拓扑”，无正式算法入口变化。

## 4.2 当前全量输入修订

- 用户明确要求不裁剪原始数据，按正常流程构建；没有锚定关系的 Segment 不替换。
- P02 local clip 与自动端点归一不再属于当前实现；T05 消费 Tool1 转换后的完整 RCSDRoad/RCSDNode。当前唯一端点例外是用户逐项确认、由 9 项白名单驱动的 copy-on-write 属性覆盖。
- `CrossLid` 与显式 `SNodeId/ENodeId` 的不一致只保留为数据证据。本轮没有获得足以把 `CrossLid` 固化为 T05 强规则的字段语义，因此不自动改写端点。
- T06 的现有 replacement plan 边界已满足“无锚定不替换”，无需放宽算法。

## 5. run04 完整输入结论

- run04 以 143 个 SWSD Node、163 条 SWSD Road、655 个 RCSDNode、469 条 RCSDRoad 完整执行主链，没有空间裁剪、端点合成或端点改写。
- 原始 SWSD 4 个缺失端点节点影响 9 条 Road；原始 RCSD 9 个缺失端点节点影响 9 条 Road。异常只审计，Tool3 仅在环岛拓扑计算中跳过相关悬挂 Road，完整 Road 继续传递。
- T01 输出 109 个 Segment；T05 的 12 条人工关系全部成功且图可消费。
- T06 Step2 形成 4 个 ready replacement plan，全部具有完整正式锚定；Step3 4/4 成功，最终 F-RCSD 为 168 条 Road、194 个 Node，最终 topology fail 0。
- `521458225_600688320` 因 `600688320` 无正式锚定在 Step1 拒绝并保留 SWSD，验证“无锚定不替换”策略生效。
- `3086610_609284657`、`521458225_612028267`、`609020493_61493884` 虽锚定完整，但分别被显式端点图连通或双向门禁拒绝；本轮未把 `CrossLid` 反推成强规则。
- 总体判定为 `passed_with_input_endpoint_exceptions`，证据见 `validation-report.md`、run04 manifest、GIS QA 和 QGIS 工程。

## 6. 历史 run03 结论（仅作对照，已被 run04 替代）

- run03 复用 run02 已验证的 `Tool1 -> Tool3 -> Tool6 -> 人工覆盖 -> Tool4 -> Tool5 -> 人工关系转换 -> T01`，只从 P02 local clip 开始重跑受影响的 T05/T06 链路。
- `5855295910117379` 通过“人工锚定 Node + CrossLid + 唯一精确端点”门控保留，工作副本端点归一到 `5855296278770582`；原始输入 hash 和 Road 几何均未变化。
- T05 的 12 条关系全部发布且图可消费，输出 RCSDRoad 从 466 增至 467；总 summary 仍仅因用户确认可忽略的局部边界端点 `5855295910117813` 为 `false`。
- T06 Step2 可替换从 4 增至 5，`609020493_61493884` 获得 5 条有序 RCSDRoad 通道；Step3 5/5 成功，最终 F-RCSD 为 161 条 Road、194 个 Node，最终拓扑失败数为 0。
- 目标 Segment 有独立的几何走廊覆盖下降审计，需 QGIS 人工复核，但不计入最终 F-RCSD 拓扑失败。
- P02 当时判定为 `passed_with_boundary_exception_and_manual_geometry_review`；该 local clip 方案已由最新完整输入约束撤销，不再是当前正式实验结果。

## 7. run05/run06 用户确认端点覆盖

- 用户明确限定拓扑只使用 RCSDRoad `SNodeId/ENodeId`，禁止 `NodeLid/CrossLid`。
- 纯显式端点图确认 `5855295910117569` 终止于缺失 Node `5855295910117804`，后续 Road `5855296278768716` 从 `5855296278770549` 出发。
- 用户逐项确认把 `5855295910117804` 临时更新为 `5855296278770549`。run05 只在完整 469 条 RCSDRoad copy-on-write 工作副本中覆盖 `5855295910117569.ENodeId`，不修改原始输入、不形成 T05/T06 通用规则。
- 用户继续确认把 `5855295910117644` 临时更新为 `5855296278770302`；原始 RCSDRoad 中旧端点只在 Road `5855295910117517.SNodeId` 唯一命中。run06 在重新从原始输入执行时同时应用两项覆盖，仍不读取 `NodeLid/CrossLid`、不执行几何推断。

## 8. run07 全量端点修正与 T06 并行走廊修复

- 用户授权把剩余 7 项精确端点错配与原 2 项合并为 9 项显式白名单；run07 对每项执行 Road 唯一、旧值精确、新 Node 唯一存在校验。469 条 RCSDRoad 的数量、ID 和几何不变，仅 9 个属性单元变化，缺失端点数从 9 降为 0。
- T05 以完整工作副本运行，12/12 人工关系全部发布且图可消费，`summary.passed=true`。
- `609020493_61493884` 的旧失败不是锚定缺失，而是 Step2 视觉单通道被大 Segment 的 ready plan 抢占后，现有逻辑只会拒绝冲突通道，不会评估大 Segment 是否存在另一条等价可通行走廊。
- run07 的首次修复错误地把几何更近的 4 段通道分给小 Segment，并用相邻 plan 的 5 段 peer 通道补足中间锚点；该结果随后被用户指出并由原始 Road 有序跟踪证据否定，不能作为最终成果。
- T06 Step2 为 7 个可替换、2 个拒绝、11 个 ready action；Step3 7/7 成功，最终 206 条 Road、243 个 Node，正式 topology fail 为 0。

## 9. run08 锚点优先级修复

- Step2 并行通道归属固定为“正式锚定关系 > required junction 有序相对位置 > 几何距离/视觉偏差”；当前 plan 必须用自身 Road 有序覆盖中间锚点，`parallel_corridor_peer_road_ids` 不再参与 Step3 局部锚点连通门禁。
- `609020493_61493884` 的 `rcsd_junc_nodes=[5855296278770685,5855296278770582]` 由 `5855295910117380 -> 5855296278768752 -> 5855296278768753 -> 5855295910117379 -> 5855296278768576` 依序覆盖；4 段通道不经过这两个锚点，回归 `3086610_609284657`。
- run08 Step2 仍为 7 个可替换、2 个拒绝、11 个 ready action；Step3 7/7 成功，F-RCSD 仍为 206 Road / 243 Node，正式 topology fail 为 0，且两个 Segment 的 peer 字段均为空、Road 归属无重复。
