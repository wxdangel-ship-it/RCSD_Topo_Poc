# 01. Introduction And Goals

`T00 Utility Toolbox` 是 `RCSD_Topo_Poc` 的内部工具集合模块。

目标：

1. 为项目内辅助工具提供统一落仓位置与固定入口。
2. 在 `Tool1-Tool7` 与 `Tool9` 范围内保持轻量、可复跑、可追溯。
3. 不把 `T00` 演化成业务生产模块或重型框架。

当前官方操作者入口位于 repo root `scripts/`；模块 `README.md` 只承担入口索引，不承担长期源事实。
