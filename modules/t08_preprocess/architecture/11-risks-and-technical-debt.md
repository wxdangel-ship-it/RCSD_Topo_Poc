# 11 风险与技术债

## 当前风险

- Tool2 Kind enrich 沿用 T00 的空间匹配口径，后续如果业务需要更强匹配规则，必须独立冻结契约。
- Tool3 当前冻结 Nodes 类型聚合，Tool4 当前冻结路口类型错误识别；自动修复与其它 Node 预处理字段语义尚未定义，不能提前实现。
- 复杂分歧 / 合流聚合依赖 Road 有向拓扑与候选字段质量，内网运行后需要通过 summary 复核聚合组。
- Tool4 连续分歧合流判定当前没有上游显式左右 road 字段，第一版使用入向夹角代理，内网运行后需要通过 summary 复核误报与漏报。

## 缓解方式

- 先用合成测试锁定格式、CRS、字段与计数行为。
- 内网真实数据运行后，通过 summary 定位未匹配与空 kind 情况。
