# 01 Introduction And Goals

## 模块目标

- 为分歧 / 合流 / 连续复杂路口建立 T04 的正式模块外壳。
- 将线程级已确认的 Step1-4 规则沉淀为 repo 内 source-of-truth。
- 提供面向后续 Step5-7 的稳定上游结果包与 Step4 目视审计输出。

## 当前正式范围

- Step1：candidate admission
- Step2：high-recall local context
- Step3：topology skeleton
- Step4：fact event interpretation

## 当前非目标

- 不实现 Step5/6/7
- 不新增 repo 官方 CLI
- 不统一 `accepted/review_required/rejected` 与 T03 `accepted/rejected` 的最终发布状态机
