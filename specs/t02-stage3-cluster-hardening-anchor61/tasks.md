# Tasks

1. 新建第三刀 SpecKit Lite 工件。
2. 在 Step6 controller 中补 cluster-local canonical flags。
3. 在 Step6 solve 中补 cluster-local audit facts / ownership facts。
4. 在 Step7 中为当前 cluster 增加 narrow de-legacy 路径。
5. 补 focused tests，覆盖：
   - nonstable center cluster
   - stable compound center cluster
   - Step5 precedence 不被误伤
6. 跑 focused tests 与 Anchor61 baseline。
7. 生成第三刀 round 输出与多角色结论。
