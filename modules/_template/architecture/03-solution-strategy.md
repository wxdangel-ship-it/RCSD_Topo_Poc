# 03 Solution Strategy

本文件是 `<module_id>` 的架构设计 / 需求具体实现策略，说明 `../SPEC.md` 中的模块需求如何落地。稳定输入输出、状态机和入口契约见 `../INTERFACE_CONTRACT.md`。

## 1. 策略总览

`<用一段业务语言说明模块如何把输入变成输出，为什么采用这条路径。>`

模块当前主链按以下业务步骤组织：

1. `<Step1：业务动作。>`
2. `<Step2：业务动作。>`
3. `<Step3：业务动作。>`

## 2. Step1 <业务步骤名称>

业务目的：

- `<说明该步骤解决什么业务问题。>`

业务原则：

- `<说明关键原则，不要只写参数。>`

实现与审计约束：

- `<说明必要实现策略、审计要求或边界。>`

输出与审计：

- `<输出或审计字段。>`

## 3. Step2 <业务步骤名称>

按 Step1 同样结构展开。

## 4. Step3 <业务步骤名称>

按 Step1 同样结构展开。

## 5. 证据和状态分层

- `<说明中间状态、正式发布状态、下游 handoff 状态、review-only 状态如何分工。>`

## 6. 实现分层

- `<file_or_component>`：`<业务职责。>`

实现构件映射只用于帮助维护，不得替代业务步骤说明。

## 7. 输出策略

- `<formal 输出。>`
- `<review-only 输出。>`
- `<internal / recovery 输出。>`

## 8. 性能与观测策略

- `<说明性能、可恢复性、progress、terminal record 或 audit 策略。>`
