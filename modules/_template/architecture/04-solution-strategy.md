# 04 方案策略

本文件是 `<module_id>` 的详细版需求说明。它面向人解释每个业务步骤如何真正落地，不应只堆伪代码、参数定义或实现文件名。

## 1. 总体策略

`<用一段中文说明模块如何把输入变成输出，为什么采用这条路径。>`

## 2. Step1：<业务步骤名称>

### 2.1 业务目的

`<说明该步骤解决什么业务问题。>`

### 2.2 输入与前提

- `<输入>`
- `<字段 / CRS / 上游状态前提>`

### 2.3 落地策略

`<用中文说明判定、锚定、转换、融合或恢复逻辑。必要时可以补充少量结构化规则，但不能只写伪代码。>`

### 2.4 输出与审计

- `<输出>`
- `<summary / audit / risk_flags>`

### 2.5 对错边界

- 对：`<正确结果>`
- 错：`<错误推断、错误 fallback、silent fix>`

## 3. Step2：<业务步骤名称>

按 Step1 同样结构展开。

## 4. Step3：<业务步骤名称>

按 Step1 同样结构展开。

## 5. 降级与失败策略

- `<失败条件>`
- `<降级方式>`
- `<诊断要求>`

## 6. 文档分工

- `README.md`：凝练版需求说明。
- `architecture/04-solution-strategy.md`：详细版需求说明。
- `INTERFACE_CONTRACT.md`：稳定输入、输出、入口、参数和验收契约。
- `architecture/05-building-block-view.md`：实现构件职责映射。
- `architecture/10-quality-requirements.md`：质量、审计、GIS / 拓扑与性能要求。
