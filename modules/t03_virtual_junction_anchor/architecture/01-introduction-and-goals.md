# 01 Introduction And Goals

## 目标

- 维护 T03 独立模块，承接冻结 `Step3 legal-space baseline` 之上的 `Step4-5` 联合阶段
- 以低结构债方式承载 Anchor61 `case-package` + Step3 baseline run root 的批量审查能力

## 未来兼容

- 当前实现以 `Step3` 为冻结前置层，并正式实现 `Step4-5`；代码与文档结构仍保留后续 `Step6-7` 的扩展位。

## 非目标

- 本轮不进入 `Step6/7`
- 本轮不迁移 T02 的 cleanup/trim 补救链
- 本轮不输出最终 topology / polygon 产物
