# T06 1885118 第一层漏斗目视审计结论归档

归档日期：2026-06-27

## 范围

本文件归档当前针对 case `1885118` 的 T06 第一层漏斗人工目视审计结论，用于后续 T03/T07/T05/T06 问题归因讨论。本文不是项目或模块 source-of-truth，不改变任何模块接口契约。

当前基线：

`/mnt/e/Work/RCSD_Topo_Poc/outputs/baselines/t10_4cases_d1fa27f_20260627_064205/e2e_full/cases/1885118`

关注漏斗：

- Segment 的路口有证据：`1243`
- Segment 的路口有锚定：`1106`
- 损失量：`137`

## 已确认审计结论

`1243 -> 1106` 这一层损失中，T05 relation 状态不可用、T05 relation 缺失两类情况，当前目视审计结论为：属于正常情况，业务上可解释为现实无 RCSD，不应直接归因为 T06 错误。

SWSD anchor 属性不可用这一类，大部分也符合现实无 RCSD 的解释。但其中存在需要继续追查的问题形态：服务区面较为特殊，T07 模块处理可能失败，数据特征表现为 `fail2`。

## 后续追查方向

1. 将 `fail2` 服务区面场景从普通无 RCSD 样本中拆出，单独形成 T07 问题池。
2. 对 `fail2` 样本优先确认：
   - T07 是否识别到服务区面；
   - T07 是否因服务区面几何、覆盖或候选关系异常导致锚定失败；
   - 下游 T05/T06 是否只是继承 T07 失败状态。
3. 保留当前漏斗解释边界：T05 relation 缺失或不可用不等价于算法失败，需结合现实 RCSD 存在性判断。

## 当前归因边界

- 可判定为正常损失：T05 relation 缺失、T05 relation 状态不可用，且目视确认现实无 RCSD。
- 需要集中追查：SWSD anchor 属性不可用中带 `fail2` 特征的服务区面场景。
- 暂不下结论：仅凭 `fail2` 字段不能直接固化为强规则，需要回到 T07 产物和原始服务区面证据继续验证。
