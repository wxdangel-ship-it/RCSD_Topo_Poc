# 任务清单

- [x] 建立隔离工作树和性能分支。
- [x] 阅读 P04 模块源事实、入口治理和 T10 性能方法。
- [x] 冻结 `main@9b20591` 业务对照基线。
- [x] 加入低开销 CPU/RSS/I/O/调用栈采样。
- [x] 新增性能采样单元测试。
- [x] 取得真实 6-Patch 优化前基线。
- [x] 定位累计占比最高的三个热点。
- [x] 优化输入发现、清单和必需图层读取。
- [x] 优化重复全网重建或重复空间扫描。
- [x] 验证并决定是否启用最多 6 worker 的有界并发。
- [x] 执行 P04 全量测试。
- [x] 执行业务成果规范化等价比对。
- [x] 执行 CRS、拓扑、几何、审计、性能五项 QA。
- [x] 执行可重复的 1500 Patch 输入读取与峰值内存规模基准。
- [x] 构建并验证内网时限、资源、QA和业务零回退只读验收器。
- [x] 提交并推送上一轮内网候选（当前第三轮热点候选仍在隔离分支，未获提交授权）。
- [x] 记录 2026-08-02 约 1500 Patch 异常终止前超过12.7小时的中间观测下界。
- [x] 为最终 DriveZone `MultiPolygon` 的原生组件增加精确等价空间索引。
- [x] 让 partial-member completion 复用共享覆盖实现。
- [x] 执行 1500 分片覆盖微基准并验证逐值完全一致。
- [x] 重新执行 P04 全量测试。
- [ ] 使用冻结 6-Patch 输入重新执行业务结果等价检查（当前本机 `Vector` 目录为空，等待恢复数据）。
- [ ] 取得第三轮约 1500 Patch 的`<=6h`且不高于异常终止前观测下界50%的正式复跑证据。
- [ ] 确认第三轮端到端耗时不高于异常终止前观测下界50%且绝对`<=6h`；
  `8h`只作为失败诊断线（等待约1532 Patch正式完成运行）。
- [x] 建立完整surface prepared终态谓词，并对边界样本回退共享精确覆盖率。
- [x] 将数值和布尔覆盖热点迁移到共享精确实现并增加有界缓存与安全路径计数。
- [x] 实现真实Patch/Segment/Junction/Node/QA/输出进度事件和`p04_progress.jsonl`。
- [x] 验证进度单调性、吞吐/ETA、10分钟停滞告警和异常可追溯性。
- [x] 执行第三轮1500分片微基准与P04全量测试。
- [x] 使用历史1885118正式成果回放真实DriveZone/Carrier覆盖热点并验证逐值一致。
- [x] 执行1500分片、50000次查询压力基准并验证逐值零差异和有界缓存。
- [x] 节流高频进度落盘并以同输入7类业务指纹证明结果不变。
- [x] 对重复Segment carrier方向走廊装配增加精确值、有界容量缓存。
- [x] 发布走廊缓存命中率、条目数、键内存和淘汰数，并加入验收硬门槛。
- [x] 以同输入完整重放验证91.8%缓存命中、7类业务指纹一致和实际工作量进度完整。
- [x] 使用完整流水线`cProfile`确认Target path重复评分为下一计算热点。
- [x] 对Target path选择增加精确输入、有界容量缓存并发布控制台收益。
- [x] 验证路径缓存89.0%命中、Segment carrier阶段降低15.38%、7类业务指纹一致。
- [x] 将路径缓存容量纳入正式验收硬门槛并完成298项P04测试。
- [x] 为Movement anchor切分和Segment access切分发布真实工作量、完成比例和业务计数。
- [x] 验证两类Movement阶段13轮调用均完整、单调，且run10相对run9的7类业务指纹一致。
- [x] 对单轮Movement carrier重复选择增加严格等价的函数内缓存和命中统计。
- [x] 验证选择缓存31.94%命中、Movement阶段降低10.54%、run11相对run10的7类业务指纹一致。
- [x] 将Movement选择缓存限制为32768条并加入逐轮验收门禁；run12相对run11的7类业务指纹一致，并完成300项P04测试。
- [x] 根据1532 Patch第二次中止日志确认首轮`segment_carrier`在`8662/46399`时仍需约6.5小时，且`terminal_fast=0`为新的全量特有热点。
- [x] 对高组件最终`MultiPolygon`复用其原生组件prepared终态谓词；正式1500组件压力基准逐值零差异，覆盖热点加速约155.40倍。
- [x] 将Patch Road居中采样改为同一GEOS算法的批量调用，保持偏移值与几何WKB逐位一致，并增加`patch_road_center`实际进度阶段。
- [x] 以最新P04 schema基线重放6-Patch完整入口；7类业务工件指纹一致、独立QA为0异常、QGIS 50层回读通过。
- [x] 完成305项P04专项测试，并将`patch_road_center`纳入正式内网验收进度门禁。
- [ ] 提交并推送当前第三轮热点候选，供内网正式复跑。
- [ ] 完成冻结6-Patch结果等价和1532 Patch正式验收。
